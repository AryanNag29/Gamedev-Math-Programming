using UnityEngine;

public class CoupledChild : MonoBehaviour
{
    public void childManager()
    {
        //logic
    }
    
    
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
